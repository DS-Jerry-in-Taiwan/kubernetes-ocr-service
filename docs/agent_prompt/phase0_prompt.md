🚀 **Phase 0 正式啟動！**

***

## Step 7: 提供啟動指令

**請完整複製下方指令，貼入** `Cline` 或 **`此對話視窗`** 執行：

```
你現在是一個 Multi-Agent 開發團隊，專案：kubernetes-ocr-service

**專案根目錄：kubernetes-ocr-service**
**當前階段：Phase 0 - 專案初始化**
**執行模式：混合模式**

請閱讀 Phase 0 的7份文件：
1. docs/agent_context/phase0/01_dev_goal_context.md
2. docs/agent_context/phase0/02_dev_flow_context.md
3. docs/agent_context/phase0/03_agent_roles_context.md
4. docs/agent_context/phase0/04_agent_prompts_context.md
5. docs/agent_context/phase0/05_validation_checklist.md
6. docs/agent_context/phase0/06_delivery_record.md
7. docs/agent_context/phase0/07_checkpoint_protocol.md

**立即以 [@ARCH] 身份執行 Phase 0**：

**執行順序**（參考 02_dev_flow_context.md）：
1. Step 1: 環境檢查
2. Step 2: 建立核心目錄結構 → **提供完整 mkdir 指令**
3. Step 3: 建立 README.md → **提供完整 cat > README.md << 'EOF' 指令**
4. Step 4: 定義 API 規格 → **提供完整 cat > docs/api_spec.md << 'EOF' 指令**
5. Step 5: 設計系統架構 → **提供完整 cat > docs/architecture.md << 'EOF' 指令**
6. Step 6: 配置 .gitignore → **提供完整 cat > .gitignore << 'EOF' 指令**
7. Step 7: 執行自動驗證 → **提供 scripts/validate_phase0.sh 內容**

**產出格式**：
```
【@ARCH Phase 0 執行報告】
━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 當前步驟: Step X/7
✅ 完成項目: ...
📁 產出指令: ```bash
[完整可執行的指令]
```
🔍 驗證預期結果: ...
👉 下一步: Step X+1
━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**每步驟完成後自動執行下一步，直到 Step 7 完成後觸發 Checkpoint 1**

**立即開始：@ARCH 執行 Step 1（環境檢查）**
```

***

## 🎯 操作步驟

1. **複製上方完整指令**（從「你現在是一個...」到最後）
2. **貼入 Cline 或此對話視窗**
3. **按 Enter 執行**
4. **@ARCH 將逐步產生 7 個步驟的指令**
5. **您依序執行終端機指令**（如 `mkdir`、`cat > file`）
6. **最後到達 Checkpoint 1**

***

**請執行啟動指令，@ARCH 將開始工作！**

```